(* int list は
 *    - []             空リスト、あるいは
 *    - first :: rest  最初の要素が first で、残りのリストが rest
 *   という形 *)

(* 目的：整数のリスト lst を受け取ったら、そのリストの長さを返す *)
(* length : int list -> int *)
let length lst = 0
